# gateway_auth (Python)

Lib Python de assinatura Ed25519 pra Portal Gateway.

> Vazia ainda. Implementação entra em PR depois do contrato (ver README raiz).

Lib recomendada: [`cryptography`](https://cryptography.io/) (Ed25519 nativo) ou [`pynacl`](https://pynacl.readthedocs.io/).
